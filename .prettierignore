# PowerShell files are not supported by prettier (no parser available)
*.ps1
*.psd1
*.psm1

# Template files with {{variables}} that break JSON/YAML parsing
**/*.template.json
**/*.template.md
**/*.template.yml

# lefthook config — inline PowerShell commands break YAML parser
config/lefthook.yml

# Third-party / generated
package-lock.json
pnpm-lock.yaml
node_modules/
