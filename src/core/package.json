{
  "name": "@gentle-vanguard/core",
  "version": "4.0.0",
  "private": true,
  "type": "module",
  "description": "Core runtime primitives for the Gentle-Vanguard stack — run-command, timeout-config, metrics-aggregator, session-context-log, resilience-bridge, operational-metrics-tracker. Replaces the runtime symlink hack (bootstrap-symlink.ts) with a real workspace package.",
  "exports": {
    "./*.js": "./*.ts",
    "./*": "./*.ts"
  }
}
