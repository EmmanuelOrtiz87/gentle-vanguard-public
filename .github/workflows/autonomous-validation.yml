name: Autonomous Validation

permissions:
  contents: read

concurrency:
  group: autonomous-validation-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 13 * * 1' # Weekly on Monday at 10:00 GMT-3 (13:00 UTC)

jobs:
  autonomous-validation:
    name: Autonomous Validation
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      - name: Configure Git
        shell: pwsh
        run: |
          git config --global core.symlinks false
          git config --global core.longpaths true

      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          fetch-depth: 0

      - name: Setup PowerShell Modules
        shell: pwsh
        run: |
          Set-PSRepository -Name PSGallery -InstallationPolicy Trusted -ErrorAction SilentlyContinue
          if (-not (Get-Module -ListAvailable -Name PowerShellGet)) {
            Install-Module -Name PowerShellGet -Force -AllowClobber -Scope CurrentUser
          }

      - name: Run Comprehensive Validation
        shell: pwsh
        run: |
          if (-not (Test-Path './scripts/utilities/WORKFLOW-ORCHESTRATION/comprehensive-validation.ps1')) {
            Write-Output "::warning::comprehensive-validation.ps1 not found — skipping validation"
            exit 0
          }
          ./scripts/utilities/WORKFLOW-ORCHESTRATION/comprehensive-validation.ps1 -Verbose

      - name: Validate Plugins
        shell: pwsh
        run: |
          if (-not (Test-Path '.\scripts\utilities\SKILLS-TOOLS\plugins-discovery.ps1')) {
            Write-Output "::warning::plugins-discovery.ps1 not found — skipping plugin validation"
            exit 0
          }
          $result = .\scripts\utilities\SKILLS-TOOLS\plugins-discovery.ps1 -Action validate -AsJson
          $parsed = $result | ConvertFrom-Json
          if ($parsed.errors -and $parsed.errors -gt 0) {
            Write-Error "Plugin validation failed: $($parsed.errors) error(s)"
            exit 1
          }
          Write-Host "Plugin validation: PASS ($($parsed.total_plugins) plugins, $($parsed.errors) errors, $($parsed.warnings) warnings)"

      - name: Validate Tool Configs
        shell: pwsh
        run: |
          if (-not (Test-Path './scripts/utilities/CONFIG/validate-tool-configs.ps1')) {
            Write-Output "::warning::validate-tool-configs.ps1 not found — skipping tool config validation"
            exit 0
          }
          ./scripts/utilities/CONFIG/validate-tool-configs.ps1

      - name: Run Cross-Workspace Validation
        shell: pwsh
        run: |
          if (-not (Test-Path './scripts/monitoring/cross-workspace-validator.ps1')) {
            Write-Output "::warning::cross-workspace-validator.ps1 not found — skipping cross-workspace validation"
            exit 0
          }
          ./scripts/monitoring/cross-workspace-validator.ps1 -Fix

      - name: Upload Validation Report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: validation-report
          path: .session/reports/

